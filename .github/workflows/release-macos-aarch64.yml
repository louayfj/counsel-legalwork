name: Release App

on:
  # A v* tag push auto-builds the release, but it is created as a DRAFT and is
  # NOT rolled out to users automatically — see the "Leave release as a draft"
  # step. Publish the draft manually in the GitHub Releases UI to go live.
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release (e.g., v0.1.2). Leave empty to use current ref."
        required: false
        type: string
      release_name:
        description: "Release title (defaults to LegalWork <tag>)"
        required: false
        type: string
      release_body:
        description: "Release notes body in Markdown (defaults to a short placeholder)"
        required: false
        type: string
      draft:
        description: "Create the GitHub Release as a draft (publish it manually in the Releases UI when ready)"
        required: false
        type: boolean
        default: true
      prerelease:
        description: "Mark the GitHub Release as a prerelease"
        required: false
        type: boolean
        default: false
      notarize:
        description: "Notarize macOS builds (requires Apple team configured)"
        required: false
        type: boolean
        default: true
      publish_sidecars:
        description: "Build + upload legalwork-orchestrator sidecar release assets"
        required: false
        type: boolean
        default: true
      publish_npm:
        description: "Publish legalwork-orchestrator/legalwork-server/opencode-router to npm if versions changed"
        required: false
        type: boolean
        default: true
      build_electron:
        description: "Build + publish Electron desktop artifacts (macOS/Linux/Windows)"
        required: false
        type: boolean
        default: true

permissions:
  actions: read
  contents: write
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  resolve-release:
    name: Resolve Release Metadata
    # SignPath trusted builds require every job leading to the Windows signing
    # request to use GitHub-hosted runners.
    runs-on: ubuntu-latest
    outputs:
      release_tag: ${{ steps.resolve.outputs.release_tag }}
      release_name: ${{ steps.resolve.outputs.release_name }}
      release_body: ${{ steps.resolve.outputs.release_body }}
      draft: ${{ steps.resolve.outputs.draft }}
      prerelease: ${{ steps.resolve.outputs.prerelease }}
      notarize: ${{ steps.resolve.outputs.notarize }}
      build_electron: ${{ steps.resolve.outputs.build_electron }}
      publish_sidecars: ${{ steps.resolve.outputs.publish_sidecars }}
      publish_npm: ${{ steps.resolve.outputs.publish_npm }}
    steps:
      - name: Resolve metadata
        id: resolve
        shell: bash
        env:
          INPUT_TAG: ${{ github.event.inputs.tag }}
          INPUT_RELEASE_NAME: ${{ github.event.inputs.release_name }}
          INPUT_RELEASE_BODY: ${{ github.event.inputs.release_body }}
          INPUT_DRAFT: ${{ github.event.inputs.draft }}
          INPUT_PRERELEASE: ${{ github.event.inputs.prerelease }}
          INPUT_NOTARIZE: ${{ github.event.inputs.notarize }}
          INPUT_BUILD_ELECTRON: ${{ github.event.inputs.build_electron }}
          INPUT_PUBLISH_SIDECARS: ${{ github.event.inputs.publish_sidecars }}
          INPUT_PUBLISH_NPM: ${{ github.event.inputs.publish_npm }}
          DEFAULT_PUBLISH_SIDECARS: ${{ vars.RELEASE_PUBLISH_SIDECARS }}
          DEFAULT_PUBLISH_NPM: ${{ vars.RELEASE_PUBLISH_NPM }}
          DEFAULT_NOTARIZE: ${{ vars.MACOS_NOTARIZE }}
          DEFAULT_BUILD_ELECTRON: ${{ vars.RELEASE_PUBLISH_ELECTRON }}
        run: |
          set -euo pipefail

          TAG_INPUT="${INPUT_TAG:-}"
          if [ -n "$TAG_INPUT" ]; then
            if [[ "$TAG_INPUT" == v* ]]; then
              TAG="$TAG_INPUT"
            else
              TAG="v$TAG_INPUT"
            fi
          else
            TAG="${GITHUB_REF_NAME}"
          fi

          if [[ ! "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ ]]; then
            echo "Invalid release tag: $TAG (expected vX.Y.Z)" >&2
            exit 1
          fi

          RELEASE_NAME_INPUT="${INPUT_RELEASE_NAME:-}"
          if [ -n "$RELEASE_NAME_INPUT" ]; then
            RELEASE_NAME="$RELEASE_NAME_INPUT"
          else
            RELEASE_NAME="LegalWork $TAG"
          fi

          RELEASE_BODY_INPUT="${INPUT_RELEASE_BODY:-}"
          if [ -n "$RELEASE_BODY_INPUT" ]; then
            RELEASE_BODY="$RELEASE_BODY_INPUT"
          else
            printf -v RELEASE_BODY '%s\n\nLegalWork %s desktop release.\n\n%s\n%s' \
              "## What's new" \
              "$TAG" \
              "- Download the attached installer for your platform." \
              "- Electron builds use the attached latest-mac.yml updater feed."
          fi

          draft="${INPUT_DRAFT:-}"
          if [ -z "$draft" ]; then
            if [ "${GITHUB_EVENT_NAME}" = "push" ]; then
              # Keep tag-triggered releases out of /releases/latest until assets + latest.json are ready.
              draft="true"
            else
              draft="false"
            fi
          fi
          prerelease="${INPUT_PRERELEASE:-false}"
          notarize="${INPUT_NOTARIZE:-}"
          if [ -z "$notarize" ]; then
            notarize="${DEFAULT_NOTARIZE:-true}"
          fi

          build_electron="${INPUT_BUILD_ELECTRON:-}"
          if [ -z "$build_electron" ]; then
            build_electron="${DEFAULT_BUILD_ELECTRON:-true}"
          fi

          publish_sidecars="${INPUT_PUBLISH_SIDECARS:-}"
          if [ -z "$publish_sidecars" ]; then
            publish_sidecars="${DEFAULT_PUBLISH_SIDECARS:-true}"
          fi
          publish_npm="${INPUT_PUBLISH_NPM:-}"
          if [ -z "$publish_npm" ]; then
            publish_npm="${DEFAULT_PUBLISH_NPM:-true}"
          fi

          TAG="${TAG//$'\n'/}"
          TAG="${TAG//$'\r'/}"
          RELEASE_NAME="${RELEASE_NAME//$'\n'/ }"
          RELEASE_NAME="${RELEASE_NAME//$'\r'/ }"

          echo "release_tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "release_name=$RELEASE_NAME" >> "$GITHUB_OUTPUT"
          echo "draft=$draft" >> "$GITHUB_OUTPUT"
          echo "prerelease=$prerelease" >> "$GITHUB_OUTPUT"
          echo "notarize=$notarize" >> "$GITHUB_OUTPUT"
          echo "build_electron=$build_electron" >> "$GITHUB_OUTPUT"
          echo "publish_sidecars=$publish_sidecars" >> "$GITHUB_OUTPUT"
          echo "publish_npm=$publish_npm" >> "$GITHUB_OUTPUT"
          {
            echo "release_body<<__LEGALWORK_RELEASE_BODY_EOF__"
            printf '%s\n' "$RELEASE_BODY"
            echo "__LEGALWORK_RELEASE_BODY_EOF__"
          } >> "$GITHUB_OUTPUT"

      - name: Create release if missing
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          BODY_FILE="$RUNNER_TEMP/release_body.md"
          printf '%s\n' "${{ steps.resolve.outputs.release_body }}" > "$BODY_FILE"

          if gh release view "${{ steps.resolve.outputs.release_tag }}" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "Release ${{ steps.resolve.outputs.release_tag }} already exists; skipping create."
            exit 0
          fi

          DRAFT_FLAG=""
          PRERELEASE_FLAG=""
          if [ "${{ steps.resolve.outputs.draft }}" = "true" ]; then
            DRAFT_FLAG="--draft"
          fi
          if [ "${{ steps.resolve.outputs.prerelease }}" = "true" ]; then
            PRERELEASE_FLAG="--prerelease"
          fi

          gh release create "${{ steps.resolve.outputs.release_tag }}" \
            --repo "$GITHUB_REPOSITORY" \
            --title "${{ steps.resolve.outputs.release_name }}" \
            --notes-file "$BODY_FILE" \
            $DRAFT_FLAG $PRERELEASE_FLAG

  verify-release:
    name: Verify Release Tag
    needs: resolve-release
    # SignPath trusted builds require every job leading to the Windows signing
    # request to use GitHub-hosted runners.
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ needs.resolve-release.outputs.release_tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ env.RELEASE_TAG }}
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      # The tag is the single source of truth for the release version; the
      # repo carries 0.0.0 placeholders and every build job stamps the tag
      # version into the workspace (scripts/release/apply-version.mjs). The
      # tag only needs to point at a commit that is already on origin/dev.
      - name: Verify release tag
        run: node scripts/release/verify-tag.mjs --tag "$RELEASE_TAG"

      - name: Stamp release version from tag
        run: node scripts/release/apply-version.mjs --tag "$RELEASE_TAG"

      - name: Release review (strict)
        run: node scripts/release/review.mjs --strict

  publish-electron:
    name: Build + publish Electron (${{ matrix.artifact }})
    needs: [resolve-release, verify-release]
    if: needs.resolve-release.outputs.build_electron == 'true'
    runs-on: ${{ matrix.platform }}
    # Apple notarization routes first-time/new-account submissions to "in-depth
    # analysis", which can run for hours until the team's notarization history
    # warms up. 120 min was too tight and cancelled the macOS jobs mid-notarize.
    # 360 is the GitHub-hosted per-job ceiling; lower it once notarize times drop.
    timeout-minutes: 360
    env:
      RELEASE_TAG: ${{ needs.resolve-release.outputs.release_tag }}
      MACOS_NOTARIZE: ${{ needs.resolve-release.outputs.notarize }}
      SIGNPATH_ORGANIZATION_ID: ${{ secrets.SIGNPATH_ORGANIZATION_ID || vars.SIGNPATH_ORGANIZATION_ID }}
      SIGNPATH_PROJECT_SLUG: ${{ secrets.SIGNPATH_PROJECT_SLUG || vars.SIGNPATH_PROJECT_SLUG }}
      SIGNPATH_SIGNING_POLICY_SLUG: ${{ secrets.SIGNPATH_SIGNING_POLICY_SLUG || vars.SIGNPATH_SIGNING_POLICY_SLUG }}
      SIGNPATH_ARTIFACT_CONFIGURATION_SLUG: ${{ secrets.SIGNPATH_ARTIFACT_CONFIGURATION_SLUG || vars.SIGNPATH_ARTIFACT_CONFIGURATION_SLUG }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-14
            os_type: macos
            artifact: electron-macos-arm64
            electron_args: "--mac --arm64"
            target_triple: aarch64-apple-darwin
          - platform: macos-14
            os_type: macos
            artifact: electron-macos-x64
            electron_args: "--mac --x64"
            target_triple: x86_64-apple-darwin
          - platform: ubuntu-22.04
            os_type: linux
            artifact: electron-linux-x64
            electron_args: "--linux --x64"
            target_triple: x86_64-unknown-linux-gnu
          - platform: ubuntu-22.04-arm
            os_type: linux
            artifact: electron-linux-arm64
            electron_args: "--linux --arm64"
            target_triple: aarch64-unknown-linux-gnu
          - platform: windows-2022
            os_type: windows
            artifact: electron-windows-x64
            electron_args: "--win --x64"
            target_triple: x86_64-pc-windows-msvc

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ env.RELEASE_TAG }}
          fetch-depth: 0

      - name: Enable git long paths (Windows)
        if: matrix.os_type == 'windows'
        shell: pwsh
        run: git config --global core.longpaths true

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.4.0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9

      - name: Get pnpm store path
        id: pnpm-store
        shell: bash
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@v5
        continue-on-error: true
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-electron-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-electron-pnpm-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # After the frozen-lockfile install; electron-builder, the updater feed,
      # and app.getVersion() all pick the stamped version up from package.json.
      # The lockfile-only resync keeps pnpm-lock.yaml consistent with the
      # stamped orchestrator dependency pins (workspace links, no re-resolution)
      # so later pnpm invocations in this job don't reject the workspace.
      - name: Stamp release version from tag
        shell: bash
        run: |
          node scripts/release/apply-version.mjs --tag "$RELEASE_TAG"
          pnpm install --lockfile-only --no-frozen-lockfile

      - name: Write Electron notary API key (macOS)
        if: matrix.os_type == 'macos' && env.MACOS_NOTARIZE == 'true'
        env:
          APPLE_NOTARY_API_KEY_P8_BASE64: ${{ secrets.APPLE_NOTARY_API_KEY_P8_BASE64 }}
          APPLE_NOTARY_API_KEY_ID: ${{ secrets.APPLE_NOTARY_API_KEY_ID }}
          APPLE_NOTARY_API_ISSUER_ID: ${{ secrets.APPLE_NOTARY_API_ISSUER_ID }}
          APPLE_CODESIGN_CERT_P12_BASE64: ${{ secrets.APPLE_CODESIGN_CERT_P12_BASE64 }}
          APPLE_CODESIGN_CERT_PASSWORD: ${{ secrets.APPLE_CODESIGN_CERT_PASSWORD }}
        run: |
          set -euo pipefail

          missing=()
          for name in \
            APPLE_NOTARY_API_KEY_P8_BASE64 \
            APPLE_NOTARY_API_KEY_ID \
            APPLE_NOTARY_API_ISSUER_ID \
            APPLE_CODESIGN_CERT_P12_BASE64 \
            APPLE_CODESIGN_CERT_PASSWORD; do
            if [ -z "${!name:-}" ]; then
              missing+=("$name")
            fi
          done
          if [ "${#missing[@]}" -gt 0 ]; then
            printf 'Missing Electron macOS notarization/signing secrets: %s\n' "${missing[*]}" >&2
            exit 1
          fi

          NOTARY_KEY_PATH="$RUNNER_TEMP/AuthKey.p8"
          printf '%s' "$APPLE_NOTARY_API_KEY_P8_BASE64" | base64 --decode > "$NOTARY_KEY_PATH"
          chmod 600 "$NOTARY_KEY_PATH"

          echo "NOTARY_KEY_PATH=$NOTARY_KEY_PATH" >> "$GITHUB_ENV"

      - name: Build Electron app
        shell: bash
        env:
          # TARGET tells prepare-sidecar.mjs which architecture's sidecars
          # to download (critical for cross-arch builds like x64 on arm64 mac).
          TARGET: ${{ matrix.target_triple }}
        run: pnpm --filter @legalwork/desktop build:electron

      - name: Package Electron (macOS, signed + notarized)
        if: matrix.os_type == 'macos' && env.MACOS_NOTARIZE == 'true'
        env:
          CSC_LINK: ${{ secrets.APPLE_CODESIGN_CERT_P12_BASE64 }}
          CSC_KEY_PASSWORD: ${{ secrets.APPLE_CODESIGN_CERT_PASSWORD }}
          APPLE_API_KEY: ${{ secrets.APPLE_NOTARY_API_KEY_ID }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_NOTARY_API_ISSUER_ID }}
          APPLE_API_KEY_PATH: ${{ env.NOTARY_KEY_PATH }}
        run: |
          set -euo pipefail
          pnpm --dir apps/desktop exec electron-builder \
            --config electron-builder.yml \
            ${{ matrix.electron_args }} \
            --publish never

      - name: Package Electron (macOS, ad-hoc signed)
        if: matrix.os_type == 'macos' && env.MACOS_NOTARIZE != 'true'
        env:
          # No Apple cert/notary secrets -> ad-hoc sign (mac.identity=-).
          # This yields a valid self-signed signature with no Apple Team ID, so
          # Gatekeeper shows the milder "developer cannot be verified -> Open
          # Anyway" prompt instead of "damaged". The hardened runtime stays on
          # because build/entitlements.mac.plist already sets
          # com.apple.security.cs.disable-library-validation. Set notarize=true +
          # add the Apple secrets to produce signed + notarized builds instead.
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        run: |
          set -euo pipefail
          echo "MACOS_NOTARIZE != 'true' -> ad-hoc signing macOS app."
          pnpm --dir apps/desktop exec electron-builder \
            --config electron-builder.yml \
            -c.mac.identity=- \
            ${{ matrix.electron_args }} \
            --publish never

      - name: Package Electron (Linux)
        if: matrix.os_type == 'linux'
        run: |
          set -euo pipefail
          pnpm --dir apps/desktop exec electron-builder \
            --config electron-builder.yml \
            ${{ matrix.electron_args }} \
            --publish never

      - name: Package Electron (Windows)
        if: matrix.os_type == 'windows'
        shell: bash
        run: |
          set -euo pipefail
          pnpm --dir apps/desktop exec electron-builder \
            --config electron-builder.yml \
            ${{ matrix.electron_args }} \
            --publish never

      - name: Validate SignPath configuration (Windows)
        if: matrix.os_type == 'windows' && env.SIGNPATH_PROJECT_SLUG != ''
        shell: bash
        env:
          SIGNPATH_API_TOKEN: ${{ secrets.SIGNPATH_API_TOKEN }}
        run: |
          set -euo pipefail

          missing=()
          for name in \
            SIGNPATH_API_TOKEN \
            SIGNPATH_ORGANIZATION_ID \
            SIGNPATH_PROJECT_SLUG \
            SIGNPATH_SIGNING_POLICY_SLUG; do
            if [ -z "${!name:-}" ]; then
              missing+=("$name")
            fi
          done

          if [ "${#missing[@]}" -gt 0 ]; then
            printf 'Missing Windows SignPath configuration: %s\n' "${missing[*]}" >&2
            exit 1
          fi

      - name: Stage unsigned Windows installer for SignPath
        if: matrix.os_type == 'windows'
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p "$RUNNER_TEMP/signpath-unsigned"
          shopt -s nullglob
          installers=(apps/desktop/dist-electron/legalwork-win-x64-*.exe)
          if [ "${#installers[@]}" -ne 1 ]; then
            printf 'Expected exactly one unsigned Windows installer, found %s.\n' "${#installers[@]}" >&2
            printf '%s\n' "${installers[@]}" >&2
            exit 1
          fi
          cp "${installers[0]}" "$RUNNER_TEMP/signpath-unsigned/"

      - name: Upload unsigned Windows installer for SignPath
        if: matrix.os_type == 'windows'
        id: upload-unsigned-windows-installer
        uses: actions/upload-artifact@v4
        with:
          name: unsigned-legalwork-windows-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/signpath-unsigned/*.exe
          if-no-files-found: error
          retention-days: 1

      - name: Submit Windows signing request to SignPath
        if: matrix.os_type == 'windows' && env.SIGNPATH_PROJECT_SLUG != '' && env.SIGNPATH_ARTIFACT_CONFIGURATION_SLUG == ''
        uses: signpath/github-action-submit-signing-request@v2
        with:
          api-token: ${{ secrets.SIGNPATH_API_TOKEN }}
          organization-id: ${{ env.SIGNPATH_ORGANIZATION_ID }}
          project-slug: ${{ env.SIGNPATH_PROJECT_SLUG }}
          signing-policy-slug: ${{ env.SIGNPATH_SIGNING_POLICY_SLUG }}
          github-artifact-id: ${{ steps.upload-unsigned-windows-installer.outputs.artifact-id }}
          wait-for-completion: true
          output-artifact-directory: ${{ runner.temp }}/signpath-signed

      - name: Submit Windows signing request to SignPath (artifact configuration)
        if: matrix.os_type == 'windows' && env.SIGNPATH_ARTIFACT_CONFIGURATION_SLUG != ''
        uses: signpath/github-action-submit-signing-request@v2
        with:
          api-token: ${{ secrets.SIGNPATH_API_TOKEN }}
          organization-id: ${{ env.SIGNPATH_ORGANIZATION_ID }}
          project-slug: ${{ env.SIGNPATH_PROJECT_SLUG }}
          signing-policy-slug: ${{ env.SIGNPATH_SIGNING_POLICY_SLUG }}
          artifact-configuration-slug: ${{ env.SIGNPATH_ARTIFACT_CONFIGURATION_SLUG }}
          github-artifact-id: ${{ steps.upload-unsigned-windows-installer.outputs.artifact-id }}
          wait-for-completion: true
          output-artifact-directory: ${{ runner.temp }}/signpath-signed

      - name: Apply signed Windows installer
        if: matrix.os_type == 'windows' && env.SIGNPATH_PROJECT_SLUG != ''
        shell: bash
        run: node scripts/release/apply-signpath-windows-artifact.mjs "$RUNNER_TEMP/signpath-signed" apps/desktop/dist-electron

      - name: Upload Electron release assets
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob
          assets=(apps/desktop/dist-electron/legalwork-*)
          if [ ${#assets[@]} -eq 0 ]; then
            echo "No Electron release assets found in apps/desktop/dist-electron" >&2
            exit 1
          fi
          gh release upload "$RELEASE_TAG" "${assets[@]}" --repo "$GITHUB_REPOSITORY" --clobber

      - name: Upload Electron updater manifest artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: apps/desktop/dist-electron/latest*.yml
          if-no-files-found: error

  publish-electron-assets:
    name: Publish Electron Assets
    needs: [resolve-release, verify-release, publish-electron]
    if: |
      always() &&
      needs.resolve-release.outputs.build_electron == 'true' &&
      needs.resolve-release.result == 'success' &&
      needs.verify-release.result == 'success' &&
      needs.publish-electron.result == 'success'
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ needs.resolve-release.outputs.release_tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          # Dispatch-based release recovery uses the workflow ref so fixes to
          # release asset publishing can run without moving an already-shipped tag.
          ref: ${{ github.event_name == 'workflow_dispatch' && github.ref_name || env.RELEASE_TAG }}
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Download Electron updater manifest artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: electron-*
          path: ${{ runner.temp }}/electron-dist

      - name: Upload merged updater manifests
        env:
          GH_TOKEN: ${{ github.token }}
        run: node scripts/release/publish-electron-assets.mjs --manifests-only "${{ runner.temp }}/electron-dist" "$RELEASE_TAG"

  release-orchestrator-sidecars:
    name: Build + Upload legalwork-orchestrator Sidecars
    needs: [resolve-release, verify-release]
    if: needs.resolve-release.outputs.publish_sidecars == 'true'
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ needs.resolve-release.outputs.release_tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ env.RELEASE_TAG }}
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.4.0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: "1.3.6"

      - name: Get pnpm store path
        id: pnpm-store
        shell: bash
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@v5
        continue-on-error: true
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ubuntu-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ubuntu-pnpm-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline

      - name: Stamp release version from tag
        shell: bash
        run: |
          node scripts/release/apply-version.mjs --tag "$RELEASE_TAG"
          pnpm install --lockfile-only --no-frozen-lockfile

      - name: Resolve sidecar versions
        id: sidecar-versions
        shell: bash
        run: |
          node -e "const fs=require('fs'); const orchestrator=JSON.parse(fs.readFileSync('apps/orchestrator/package.json','utf8')); const server=JSON.parse(fs.readFileSync('apps/server/package.json','utf8')); const opencodeRouter=JSON.parse(fs.readFileSync('apps/opencode-router/package.json','utf8')); console.log('orchestrator=' + orchestrator.version); console.log('server=' + server.version); console.log('opencodeRouter=' + opencodeRouter.version);" >> "$GITHUB_OUTPUT"

      - name: Resolve SOURCE_DATE_EPOCH
        id: source-date
        shell: bash
        run: |
          epoch=$(git show -s --format=%ct "${RELEASE_TAG}")
          echo "epoch=$epoch" >> "$GITHUB_OUTPUT"

      - name: Check legalwork-orchestrator release
        id: orchestrator-release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          tag="legalwork-orchestrator-v${{ steps.sidecar-versions.outputs.orchestrator }}"
          if gh release view "$tag" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Build orchestrator release artifacts
        env:
          SOURCE_DATE_EPOCH: ${{ steps.source-date.outputs.epoch }}
        run: |
          pnpm --filter legalwork-orchestrator build:bin:all
          pnpm --filter legalwork-orchestrator build:sidecars

      - name: Release review (strict)
        env:
          SOURCE_DATE_EPOCH: ${{ steps.source-date.outputs.epoch }}
        run: node scripts/release/review.mjs --strict

      - name: Create legalwork-orchestrator release
        if: steps.orchestrator-release.outputs.exists != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version="${{ steps.sidecar-versions.outputs.orchestrator }}"
          tag="legalwork-orchestrator-v${version}"
          notes="Sidecar bundle for legalwork-orchestrator v${version}.\n\nlegalwork-server: ${{ steps.sidecar-versions.outputs.server }}\nopencodeRouter: ${{ steps.sidecar-versions.outputs.opencodeRouter }}"
          gh release create "$tag" \
            --repo "$GITHUB_REPOSITORY" \
            --title "legalwork-orchestrator v${version}" \
            --notes "$notes" \
            --latest=false

      - name: Upload orchestrator release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="legalwork-orchestrator-v${{ steps.sidecar-versions.outputs.orchestrator }}"
          gh release upload "$tag" apps/orchestrator/dist/bin/* apps/orchestrator/dist/sidecars/* --repo "$GITHUB_REPOSITORY" --clobber

  publish-npm:
    name: Publish npm packages
    needs: [resolve-release, verify-release, release-orchestrator-sidecars]
    if: |
      always() &&
      needs.resolve-release.result == 'success' &&
      needs.verify-release.result == 'success' &&
      (needs.release-orchestrator-sidecars.result == 'success' || needs.release-orchestrator-sidecars.result == 'skipped') &&
      needs.resolve-release.outputs.publish_npm == 'true'
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ needs.resolve-release.outputs.release_tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          # Dispatch-based release recovery uses the workflow ref so fixes to
          # npm release automation can run without moving an already-shipped tag.
          ref: ${{ github.event_name == 'workflow_dispatch' && github.ref_name || env.RELEASE_TAG }}
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.4.0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: "1.3.6"

      - name: Get pnpm store path
        id: pnpm-store
        shell: bash
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@v5
        continue-on-error: true
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ubuntu-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ubuntu-pnpm-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --prefer-offline

      - name: Stamp release version from tag
        shell: bash
        run: |
          node scripts/release/apply-version.mjs --tag "$RELEASE_TAG"
          pnpm install --lockfile-only --no-frozen-lockfile

      - name: Resolve package versions
        id: package-versions
        shell: bash
        run: |
          node -e "const fs=require('fs'); const orchestrator=JSON.parse(fs.readFileSync('apps/orchestrator/package.json','utf8')); const server=JSON.parse(fs.readFileSync('apps/server/package.json','utf8')); const opencodeRouter=JSON.parse(fs.readFileSync('apps/opencode-router/package.json','utf8')); console.log('orchestrator=' + orchestrator.version); console.log('server=' + server.version); console.log('opencodeRouter=' + opencodeRouter.version);" >> "$GITHUB_OUTPUT"

      - name: Check npm versions
        id: npm-versions
        shell: bash
        env:
          ORCHESTRATOR_VERSION: ${{ steps.package-versions.outputs.orchestrator }}
          SERVER_VERSION: ${{ steps.package-versions.outputs.server }}
          OPENCODE_ROUTER_VERSION: ${{ steps.package-versions.outputs.opencodeRouter }}
        run: |
          set -euo pipefail
          # npm view exits non-zero for packages that don't exist yet (404).
          # Treat missing packages as "not published" so release can publish them.
          orchestrator_current="$(npm view legalwork-orchestrator version 2>/dev/null || true)"
          server_current="$(npm view legalwork-server version 2>/dev/null || true)"
          opencodeRouter_current="$(npm view opencode-router version 2>/dev/null || true)"

          if [ "$orchestrator_current" = "$ORCHESTRATOR_VERSION" ]; then
            echo "publish_orchestrator=false" >> "$GITHUB_OUTPUT"
          else
            echo "publish_orchestrator=true" >> "$GITHUB_OUTPUT"
          fi

          if [ "$server_current" = "$SERVER_VERSION" ]; then
            echo "publish_server=false" >> "$GITHUB_OUTPUT"
          else
            echo "publish_server=true" >> "$GITHUB_OUTPUT"
          fi

          if [ "$opencodeRouter_current" = "$OPENCODE_ROUTER_VERSION" ]; then
            echo "publish_opencodeRouter=false" >> "$GITHUB_OUTPUT"
          else
            echo "publish_opencodeRouter=true" >> "$GITHUB_OUTPUT"
          fi

          publish_any=false
          if [ "$orchestrator_current" != "$ORCHESTRATOR_VERSION" ] || [ "$server_current" != "$SERVER_VERSION" ] || [ "$opencodeRouter_current" != "$OPENCODE_ROUTER_VERSION" ]; then
            publish_any=true
          fi
          echo "publish_any=$publish_any" >> "$GITHUB_OUTPUT"

      - name: Ensure npm auth
        id: npm-auth
        shell: bash
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          PUBLISH_ANY: ${{ steps.npm-versions.outputs.publish_any }}
        run: |
          set -euo pipefail

          if [ "${PUBLISH_ANY}" != "true" ]; then
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ -z "${NPM_TOKEN:-}" ]; then
            echo "NPM_TOKEN not set; skipping npm publish."
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          npm config set //registry.npmjs.org/:_authToken "$NPM_TOKEN"
          echo "enabled=true" >> "$GITHUB_OUTPUT"

      - name: Publish legalwork-server
        if: steps.npm-auth.outputs.enabled == 'true' && steps.npm-versions.outputs.publish_server == 'true'
        run: pnpm --filter legalwork-server publish --access public --no-git-checks

      - name: Publish opencode-router
        if: steps.npm-auth.outputs.enabled == 'true' && steps.npm-versions.outputs.publish_opencodeRouter == 'true'
        run: pnpm --filter opencode-router publish --access public --no-git-checks

      - name: Publish legalwork-orchestrator
        if: steps.npm-auth.outputs.enabled == 'true' && steps.npm-versions.outputs.publish_orchestrator == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          ORCHESTRATOR_VERSION: ${{ steps.package-versions.outputs.orchestrator }}
        run: |
          set -euo pipefail
          tag="legalwork-orchestrator-v${ORCHESTRATOR_VERSION}"
          if ! gh release view "$tag" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "legalwork-orchestrator sidecar release $tag not found. Publish sidecars before legalwork-orchestrator." >&2
            exit 1
          fi
          pnpm --filter legalwork-orchestrator build:bin:all
          node apps/orchestrator/scripts/publish-npm.mjs

  publish-release:
    name: Publish GitHub Release
    needs:
      - resolve-release
      - verify-release
      - publish-electron
      - publish-electron-assets
      - release-orchestrator-sidecars
      - publish-npm
    if: |
      always() &&
      needs.resolve-release.result == 'success' &&
      needs.verify-release.result == 'success' &&
      (needs.publish-electron.result == 'success' || needs.publish-electron.result == 'skipped') &&
      (needs.publish-electron-assets.result == 'success' || needs.publish-electron-assets.result == 'skipped') &&
      (needs.release-orchestrator-sidecars.result == 'success' || needs.release-orchestrator-sidecars.result == 'skipped') &&
      (needs.publish-npm.result == 'success' || needs.publish-npm.result == 'skipped')
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ needs.resolve-release.outputs.release_tag }}
      RELEASE_PRERELEASE: ${{ needs.resolve-release.outputs.prerelease }}
    steps:
      - name: Leave release as a draft for manual publish
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          # Intentionally do NOT auto-publish. The build attaches all installers +
          # updater manifests to a DRAFT release; rolling it out to users is a
          # deliberate manual step — open the release in the GitHub UI and click
          # "Publish release" (set as latest). Until then /releases/latest does
          # not serve it, so no installed app auto-updates.
          is_draft="$(gh release view "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" --json isDraft -q .isDraft)"
          if [ "$is_draft" = "true" ]; then
            echo "Draft release $RELEASE_TAG is ready — publish it manually in the GitHub Releases UI to roll out to users."
          else
            echo "Release $RELEASE_TAG is already published; nothing to do."
          fi
